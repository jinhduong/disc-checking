sudo: required
addons:
  chrome: stable

node_js:
  - 8
  - 9
cache:
  directories:
  - node_modules

branches:
  only:
    - master

before_script:
- npm install -g @angular/cli
- npm install

script:
  - ng test